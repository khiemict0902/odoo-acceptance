<odoo>
    <data>
        <record id="action_approve_timeoff_batch" model="ir.actions.server">
            <field name="name">Phê duyệt nghỉ phép</field>
            <field name="model_id" ref="hr_holidays.model_hr_leave"/>
            <field name="binding_model_id" ref="hr_holidays.model_hr_leave"/>
            <field name="state">code</field>
            <field name="code">
                action = records.action_approve_batch()
            </field>
            <field name="binding_view_types">tree</field>
        </record>

    </data>
</odoo>
